# -----------------------------------------------------------------------------
# ASB Platform ECS Environment Example
# -----------------------------------------------------------------------------
# Copy this file to env/ecs.env on the ECS host and edit all values as needed.
# Never commit real secrets or ACR credentials into Git.
# -----------------------------------------------------------------------------

# --- General Registry Settings ------------------------------------------------
ACR_REGISTRY=registry.cn-hangzhou.aliyuncs.com  # TODO: adjust to your actual region
ACR_NAMESPACE=asbsecurity

# --- ASB Image References (override tags per release) ------------------------
ASB_CP_BACKEND_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/cp-backend:latest
ASB_CP_FRONTEND_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/cp-frontend:latest
ASB_GATEWAY_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/asb-secure-gateway:latest
ASB_AUTH_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/auth:latest
ASB_POLICY_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/policy:latest
ASB_PRIVACY_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/privacy:latest
ASB_PROMPT_DEFENSE_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/prompt-defense:latest
ASB_OUTPUT_GUARD_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/output-guard:latest
ASB_AGENT_CONTROL_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/agent-control:latest
ASB_AUDIT_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/audit:latest
ASB_EXAMPLE_SERVICE_IMAGE=${ACR_REGISTRY}/${ACR_NAMESPACE}/example-service:latest

# --- Host Port Mappings ------------------------------------------------------
CP_BACKEND_PORT=8080
CP_FRONTEND_PORT=80
GATEWAY_PORT=8000
KEYCLOAK_PORT=8081
CLICKHOUSE_HTTP_PORT=8123

# --- Postgres Credentials ----------------------------------------------------
POSTGRES_USER=asb
POSTGRES_PASSWORD=asbpass
POSTGRES_DB=asb_platform

# --- Keycloak Admin ----------------------------------------------------------
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_me

# --- Optional Aliyun ACR Login (set directly on ECS host) --------------------
ACR_USERNAME=changeme-acr-user
ACR_PASSWORD=changeme-acr-pass

# NOTE:
#   1. After copying this file to env/ecs.env, replace all placeholder values.
#   2. Keep env/ecs.env out of version control (git ignore it).
#   3. Rotate passwords/client secrets regularly.

